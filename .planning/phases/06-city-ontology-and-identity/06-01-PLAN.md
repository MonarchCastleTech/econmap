# Phase 6 Plan 01: City identity contract

## Tasks

1. Add failing parser fixtures for requested cities, subordinate counterexamples, Unicode slugs, and California.
2. Add `placeClass`, GeoNames feature metadata, and canonical source IDs at ingestion.
3. Add failing shared-schema tests and preserve legacy generated-record compatibility.
4. Add explicit observed/not-observed/unknown/unavailable coverage states.
5. Add failing search-ranking and slim-index publication tests.
6. Rank cities before subordinate places and preserve identity fields through published/fallback indexes.
7. Extract linked Wikidata and OpenStreetMap IDs from GeoNames alternate-name records.
8. Verify focused tests, existing ingestion tests, typecheck, and targeted lint.

## Verification

- `npm run test --` focused Phase 6 suites
- `npm run test -- scripts/data/cities/ingest-registry.test.ts scripts/data/cities/ingest-registry.publishable.test.ts`
- `npm run typecheck`
- targeted ESLint over changed Phase 6 files
